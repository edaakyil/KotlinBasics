/*
Sınıf Çalışması: Parametresi ile aldığı Int türden bir dizinin elemanlarını stdout'a yazdıran printArray isimli
aşağıdaki fonksiyonu ve randomIntArray isimli aşağıdaki fonksiyonu sırasıyla console.kt ve arrayUtil.kt içerisinde
yazınız ve aşağıdaki kod ile test ediniz.
Fonksiyonlar:
    fun printArray(a: DoubleArray, n: Int = 1, sep: String = " ", end: String = "\n")
    fun randomArray(count: Int, origin: Int, bound: Int, random: Random = Random): IntArray
 */

package com.edaakyil.studywork.array.sw1
/*
import com.edaakyil.kotlin.util.array.generateRandomArray
import com.edaakyil.kotlin.util.console.printArray

fun main() = runArrayTest()

fun runArrayTest() {
    while (true) {
        print("Input count of array created: ")
        val count = readln().toInt()

        if (count < 0)
            break

        print("Input origin value: ")
        val origin = readln().toDouble()

        print("Input bound value: ")
        val bound = readln().toDouble()

        val arr: DoubleArray = generateRandomArray(count, origin, bound)

        printArray(arr, n = 3)
        println("----------------------------")
    }
}
 */